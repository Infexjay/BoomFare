{
  "name": "boomfare-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "frontend": "yarn workspace frontend dev",
    "backend": "yarn workspace backend start",
    "dev": "npx concurrently \"yarn:frontend\" \"yarn:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
